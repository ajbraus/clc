<div id="sideways">
	<div class="scene" id="hero">

	<h1>Your Personalized Cost of Living Calculator</h1>
	  <div class="half-width pull-left">
	  	<h3>
	  		Are you the same as your neighbor?
	  	</h3>
	  	<div class="lead">
	  		The Bureau of Labor Statistics thinks so. They treat everyone the same.
	  	</div>
	  	<div class="lead">
	  		At Cost of Living Card we believe that your cost of living is personal.
	  	</div>
	  </div>
	  <div class="pull-right half-width push-up">
	  	<%= image_tag 'blsseesyou.jpg' %>
	  	<div class="text-center">
	  		<%= link_to "Get Started (Its Free!)", "#problem", class:'btn btn-large btn-primary next' %>
	  	</div>
	  </div>
	</div>

	<div class="scene" id="problem">
		<h2>Spending Breakdown</h2>
		<div class="pull-left spending">
			<ul>
				<li>
					<%= label_tag :apparel, "Apparel" %>
	  			<%= text_field_tag :apparel, nil, autocomplete: "off", class:"weight-field" %>
				</li>
				<li>
					<%= label_tag :food, "Groceries" %>
	  			<%= text_field_tag :food, nil, autocomplete: "off", class:"weight-field" %>
				</li>
				<li>
					<%= label_tag :tech, "Technology" %>
	  			<%= text_field_tag :tech, nil, autocomplete: "off", class:"weight-field" %>
				</li>
				<li>
					<%= label_tag :medical, "Medical" %>
	  			<%= text_field_tag :medical, nil, autocomplete: "off", class:"weight-field" %>
				</li>
				<li>
					<%= label_tag :education, "Education" %>
	  			<%= text_field_tag :education, nil, autocomplete: "off", class:"weight-field" %>
				</li>			
				<li>
					<%= label_tag :personal_care, "Personal Care" %>
	  			<%= text_field_tag :personal_care, nil, autocomplete: "off", class:"weight-field" %>
				</li>
				<li>
					<%= label_tag :recreation, "Recreation" %>
	  			<%= text_field_tag :recreation, nil, autocomplete: "off", class:"weight-field" %>
				</li>
				<li>
					<%= label_tag :transport, "Transport" %>
	  			<%= text_field_tag :transport, nil, autocomplete: "off", class:"weight-field" %>
				</li>
				<li>
					<%= label_tag :housing, "Housing" %>
	  			<%= text_field_tag :housing, nil, autocomplete: "off", class:"weight-field" %>
				</li>
			</ul>
			<%= link_to "Submit", "#solution", class:'btn btn-large btn-success next' %>
			<div class="muted">*Inital values are the CPI's Standard Urban Consumer</div>
		</div>
	  <%= high_chart("pieChart", @chart) do |c| %>
      <%= raw "options.tooltip.formatter = function() {return this.point.name +'</b>: '+ this.y +' %';}"  %>
      <%= raw "options.plotOptions.pie.dataLabels.formatter = function() { if (this.y > 5) return this.point.name; }"  %>
    <% end %>
	</div>

	<div class="scene" id="solution">

	<h1>It doesn't add up.</h1>
	<p>When they say that inflation is only 1.5%. <br> <br>
		In reality inflation is different for everyone, and for most it is  between <strong>3-5%</strong>.</p> 

	<h1>Your one of Millions</h1>
		<p>Since 2007 US households have increased their savings <strong>300%.</strong><br><br>
			Over the same time, interest rates fell to <strong>record lows.</strong>
		</p>


	<h1>Pay Anywhere, Save Everywhere</h1>
	<p>We provide a <strong>universal payment network</strong>,<br><br> and we aggregate living costs across the US <strong>your savings</strong> travel with you.</p>

		<%= link_to "Get More Information", "#join", class:'btn btn-large next pull-right' %>
	</div>

	<div class="scene" id="join">

		<h2>Stay In Touch</h2>
	<%= form_for @email_subscription do |f| %>
	  <% if @email_subscription.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@email_subscription.errors.count, "error") %> prohibited this email_subscription from being saved:</h2>

	      <ul>
	      <% @email_subscription.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

	  <%= f.label :email %></br>
		<%= f.text_field :email %><br>
		<%= f.submit "Stay in Touch", class:"btn btn-success" %>
	<% end %>
	</div>
</div>